SELECT ?content ?content_updated ?credit ?credit_updated WHERE {
  ?content wdt:P1877 ?credit; # after a work by
           schema:dateModified ?content_updated.
  ?credit schema:dateModified ?credit_updated.
}