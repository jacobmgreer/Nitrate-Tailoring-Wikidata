SELECT ?item ?prop ?value ?date WHERE {
  VALUES ?prop { 
    wdt:P3265 # Myspace
    wdt:P7085 # Tiktok Username
    wdt:P2002 # Twitter/X
    wdt:P3185 # VK
  }
  ?item ?prop ?value;
        schema:dateModified ?date.
}