SELECT ?item ?prop ?value ?date WHERE {
  VALUES ?prop { 
    wdt:P4003 # Facebook Page
    wdt:P2013 # Facebook User
    wdt:P2003 # Instagram
  }
  ?item ?prop ?value;
        schema:dateModified ?date.
}